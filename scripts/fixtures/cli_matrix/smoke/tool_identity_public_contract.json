{
  "id": "tool_identity_public_contract",
  "kind": "smoke",
  "description": "Ask the client-facing model surface to describe available editing tools and fail unless public output both confirms apply_patch and avoids internal transport prefixes.",
  "prompt": "Briefly list the exact public names of any editing tools you can use in this environment. Your answer must include apply_patch if that editing tool is available. Never include internal transport prefixes or proxy-private tool names.",
  "verifier": {
    "type": "stdout_contract",
    "contains_any": [
      "apply_patch"
    ],
    "not_contains": [
      "__llmup_custom__"
    ]
  },
  "timeout_secs": 90
}
